{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "command": "cmd /c \"cd ${fileDirname} && gcc ${fileBasename} -o Launcher && Launcher.exe && pause && exit 0\";",
            "request": "launch",
            "name": "C"
        }
    ]
}
